# spacex-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build spacex-types` to build the library.

## Running unit tests

Run `nx test spacex-types` to execute the unit tests via [Vitest](https://vitest.dev/).
